import {
  UserControl,
  MatchControl,
  TeamControl,
  LeaderboardControl,
} from '../controller';

const userControl = new UserControl();
const matchControl = new MatchControl();
const teamControl = new TeamControl();
const leaderboardControl = new LeaderboardControl();

export { userControl, matchControl, teamControl, leaderboardControl };
